# Email gadget, bootable with QEMU. Run using:
# kas shell -c "runqemu tmp/deploy/images/qemux86-64/qemu-email-gadget-qemux86-64.rootfs.wic wic serial nographic" kas/email-qemu.yaml
header:
  version: 17
  includes:
    - kas/email.yaml
local_conf_header:
  debug: |
    EXTRA_IMAGE_FEATURES += "debug-tweaks"
