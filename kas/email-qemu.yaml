# Email gadget, bootable with QEMU. Run using:
# kas shell -c "runqemu qemux86-64 serial nographic bootparams=ro" kas/email-qemu.yaml
header:
  version: 17
  includes:
    - kas/email.yaml
local_conf_header:
  debug: |
    EXTRA_IMAGE_FEATURES += "debug-tweaks"
